{
  "name": "openlogs",
  "version": "0.0.2-beta.1",
  "description": "Run local dev commands in a PTY and mirror their logs for LLMs.",
  "license": "MIT",
  "type": "module",
  "bin": {
    "openlogs": "./src/cli.ts",
    "ol": "./src/cli.ts"
  },
  "scripts": {
    "test": "bun test",
    "bump": "npm version prerelease --preid=beta --workspaces=false && npm publish --tag beta",
    "patch": "npm version patch --workspaces=false && npm publish --tag latest"
  }
}
